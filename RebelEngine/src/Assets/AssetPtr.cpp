#include "Engine/Framework/EnginePch.h"
#include "Engine/Assets/AssetPtr.h"
/*#include "Engine/Framework/BaseEngine.h"
#include "Engine/Assets/AssetManagerModule.h"*/

/*template <typename T>
T* AssetPtr<T>::Get() const
{

	if (!Ptr || GEngine->GetModuleManager().GetModule<AssetManagerModule>()->GetManager().IsLoaded(Handle, Ptr))
	{
		Ptr = static_cast<T*>(GEngine->GetModuleManager().GetModule<AssetManagerModule>()->GetManager().Load(Handle));
	}
	return Ptr;
	
}*/




